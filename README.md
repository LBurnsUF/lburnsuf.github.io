# lburnsuf.github.io

Personal e-portfolio: research, coursework, and the URSP journal. Plain static
HTML/CSS, no build step. Migrated from Wix, July 2026.

Pages are hand-editable. `scripts/gen_site.py` regenerates all pages from its
content data if bulk changes are needed (edit the script, run it, commit the
HTML it writes).
